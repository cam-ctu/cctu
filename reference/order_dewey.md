# sorts inputs that look like Dewey decimal numbering

sorts inputs that look like Dewey decimal numbering

## Usage

``` r
order_dewey(x)
```

## Arguments

- x:

  a vector of character strings

## Value

a numeric vector that sorts the input as per dewey decimal system,
without confusing 10.2, vs 10.10
