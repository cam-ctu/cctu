# Print methods for code_tree object S3 class inheritance means we dont' need this at all it will default to print.data.frame

Print methods for code_tree object S3 class inheritance means we dont'
need this at all it will default to print.data.frame

## Usage

``` r
find_common_prefix(vec)
```
