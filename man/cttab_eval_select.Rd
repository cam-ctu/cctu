% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cttab_helpers.R
\name{cttab_eval_select}
\alias{cttab_eval_select}
\title{Evaluate a `select` filter expression for one variable.}
\usage{
cttab_eval_select(data, var, select)
}
\arguments{
\item{data}{A `data.frame` / `data.table` providing the evaluation
environment.}

\item{var}{Name of the variable whose filter should be evaluated.}

\item{select}{Named filter list (or `NULL`).}
}
\value{
Logical vector of length `nrow(data)`. Returns all-`TRUE` when
  `select` is `NULL` or when `var` has no entry in `select`. On
  evaluation error a warning is emitted and all-`TRUE` is returned.
}
\description{
`select` is a named character vector / list whose names are variable
names and whose values are R-expression strings evaluated against
`data`. The contract matches `stat_tab()`: rows where the expression is
`FALSE` or `NA` are excluded.
}
\keyword{internal}
