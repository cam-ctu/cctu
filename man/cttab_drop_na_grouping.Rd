% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cttab_helpers.R
\name{cttab_drop_na_grouping}
\alias{cttab_drop_na_grouping}
\title{Drop rows with missing values in any grouping / row-split column.}
\usage{
cttab_drop_na_grouping(data, cols)
}
\arguments{
\item{data}{A `data.table`.}

\item{cols}{Column names to check. `NULL` entries are silently skipped
so `c(group, row_split)` works when one is `NULL`.}
}
\value{
A `data.table` with the offending rows removed.
}
\description{
Returns a new `data.table` (does not mutate `data` in place). Emits a
`message()` listing the dropped count when any rows are removed so the
user is aware their group / row-split column carried `NA`s.
}
\keyword{internal}
